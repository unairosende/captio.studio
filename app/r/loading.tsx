/**
 * The client waits on the same database the team does, and knows the product
 * less: the skeleton the signed-in pages show serves the link's pages too.
 */
export { default } from '@/app/(app)/loading'
